{
  "name": "sigma-avatars-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "dev": "cd demo && bun dev",
    "build": "cd packages/sigma-avatars && bun run build",
    "build:demo": "cd demo && bun run build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "typescript": "~5.9.2"
  }
}